sh echo "Adding 200ms delay to Server 1"
server1 tc qdisc add dev server1-eth0 root netem delay 200ms

s1 tcpdump -i any host 10.0.0.5 -n -U -w test2_startcongestion.pcap &

sh echo "Starting simulate user"
client python3 simulate_users.py

sh echo "Waiting few seconds for last segments"
sh sleep 5

sh echo "Performing cleanup"
sh pkill tcpdump
server1 tc qdisc del dev server1-eth0 root

sh echo "Test 2 - Congestion (Start) - Complete. Check test1_startcongestion.pcap"